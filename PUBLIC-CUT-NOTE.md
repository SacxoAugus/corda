# PUBLIC CUT NOTE

This repository is the **public cut** of CORDA. It is a derived
artifact of the maintainer's private canonical archive, generated by a
deterministic cut script kept in the private archive (it embeds the
redaction map, which names the private subject).

What this means, honestly:

- The **tool** (skill, references, scripts, synthetic benchmarks) is
  complete and verbatim. `python3 scripts/verify_repo.py` passes
  standalone (the development-universe rebuild gate reports SKIP here
  because the development journal is private).
- The **development journal** (`runs/`) is private: it is canonical
  evidence in Portuguese, and it embeds material from a real
  field-test subject that the owner chose not to publish.
- Four audit documents mention the field-test subject and ship as
  **redacted copies** (mechanical name map below). Their canonical
  originals remain in the private archive; original hashes are listed
  so any reviewer granted private access can verify integrity.
- The audit trail is real: an external cross-model auditor
  (`gpt-5.6-sol`) failed this project's promotion twice and executed
  an isolated adversarial gate that failed a candidate the nominal
  battery had approved. Those documents are here because the method's
  credibility is the falsification trail, not the green checkmarks.

## Redaction (mechanical, declared)

- 10 ordered mechanical replacements were applied.
- The map itself names the private field-test subject, so it lives in
  the private archive (cut script); map sha256 (first 16): `fcc26fd388762fd7`.
- Replacement tokens visible in the public copies: `[field universe]`, `[field-install]`, `[field-project-team]`, `[field-project]`, `[field-project]/[field-install]`, `[field-team]`, `[field-universe]`.

## Redacted files (original sha256:16 → public sha256:16)

- `docs/audits/SOLICITACAO-parecer-v4-pos-campo-codex-sol.md`: `c6d3021f296edc77` → `bfdb5276e9839bf6`
- `docs/audits/v4-parecer-pos-campo-codex-sol.md`: `24bb12aa61c35d81` → `62a15edf07ca138d`
- `docs/audits/SOLICITACAO-gate-isolado-v4-ciclo-07-codex-sol.md`: `ecdf918eb5a7bf1c` → `f9fd98123b8ebc5d`
- `docs/audits/v4-gate-isolado-ciclo-07-codex-sol.md`: `6386776d3b90284b` → `fb804d083770be27`
- `docs/audits/SOLICITACAO-critica-artigo-rev2-codex-sol.md`: `6c388dc3c1f0851e` → `21910179416d3e04`
- `docs/audits/artigo-rev2-critica-codex-sol.md`: `97b0951c635abe52` → `87be00e19f150778`
- `docs/audits/artigo-rev2-critica-REPRODUCAO-integrador.md`: `890307397c8e3818` → `06dfcfb812b804cd`

## Excluded from the public cut (declared)

- `runs/` — development journal (canonical evidence, Portuguese) — private
- `docs/briefings/` — field-subject briefing — private (owner decision)
- `docs/audits/v4-parecer-pos-campo-codex-sol.md (original)` — public copy is redacted; original canonical in private archive
- `docs/audits/v4-gate-isolado-ciclo-07-codex-sol.md (original)` — public copy is redacted; original canonical in private archive
- `docs/artigo-brief.md` — paper handoff briefing — private
- `paper/` — preprint working tree — private until submission

Promotion status at cut time: the v4 candidate is NOT promoted.
Pending: cross-model re-gate of the latest repair, sealed holdout,
human visual review, and explicit human acceptance. The tool works;
the claims are narrow by design (see README).
